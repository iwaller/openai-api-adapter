services:
  openai-api-adapter:
    build: .
    ports:
      - "6600:6600"
    environment:
      - PORT=6600
      - DEBUG=false
      - DEFAULT_PROVIDER=claude
      # - CLAUDE_BASE_URL=https://api.anthropic.com
      # Aiberm settings
      # - AIBERM_BASE_URL=https://your-aiberm-api.com/v1
    restart: unless-stopped
